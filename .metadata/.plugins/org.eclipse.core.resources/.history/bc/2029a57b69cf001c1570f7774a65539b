package ejercicio;

//	2022-05-06 (19)

public class Title {
	/*
	1. Dado el siguiente fichero de autores de tipo csv realiza las siguientes tareas:
		Crea un proyecto maven.
		Lee y almacena los objetos en una lista o en un mapa con clave número de registro.
		Crea la base de datos SQLite 'libreria1.db'  y graba todos los objetos del mapa en la tabla 'autores' cuyas columnas vienen dadas por la cabecera del fichero csv.
		Serializa la tabla a un fichero de objetos de autores.
		Otras que se te ocurran.

	2. Incorpora al proyecto anterior la dependencia para gestionar JSON.
		Lee el fichero json "autores.json" (es un array) proporcionado y haz lo mismo que antes.

	3. Ahora lee el archivo "libreria_dos.json":
		Carga en una lista los autores con sus respectivos libros y almacénalos en la base de datos.
		.

	4.
		Crea una clase POJO con cinco o más propiedades de diversos tipos: constructores, getter, setter, operaciones necesarias, ...
		Crea una clase Conexión a la base de datos, con patrón Singletony fichero Properties(con usuario/pass si usas MariaDB).
		Partiendo de la clase ABC definida previamente, crea una clase DAO con las operaciones de:
		Buscar un ABC: se le pasará ‘id’ y devolverá un objeto ABC.
		Borrar ABC: se le pasará ‘id’ y devolverá boolean (encontrado y borrado o no).
		Insertar ABC: se le pasarán todos los datos y y devolverá booleansegún lo haya encontrado e insertado o no.
		Listar ABCs: devuelve un ArrayList de ABCs, con todos los registros de la tabla.
		Otras operaciones.
		Finalmente, hacer un programa con un menú, que permita: buscar, borrar, insertar y listar.
		MVC
		 
		autores.csv 				26 de abril de 2022, 09:53
		 
		autores.json 				30 de abril de 2022, 10:35
		 
		libreria_dos.json 			2 de mayo de 2022, 10:00
	*/
}
