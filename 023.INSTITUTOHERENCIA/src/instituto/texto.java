package instituto;

public enum texto {
	/*
	public enum texto {
		1.
		Crea una clase Persona con las propiedades: nif, nombre, fecha nacimiento, ...
		Crea una clase Alumno con las propiedades: número de matrícula, curso, grupo, notas de tres asignaturas, nif, nombre, fecha nacimiento, ...
		Crea una clase Profesor con las propiedades: especialidad, turno (M o T), nif, nombre, fecha nacimiento, ...
		Escribe los constructores necesarios, los métodos get y set para todas las propiedades, los métodos toString, ...

		1a.

		Haz los cambios necesarios en el ejercicio anterior para que la clase Persona sea superclase de las clases Alumno y Profesor.
		Modifica los métodos toString para que use la superclase.

	}
	*/
}
